apiVersion: apps/v1
kind: Deployment
metadata:
  name: gptworker-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gptworker
  template:
    metadata:
      labels:
        app: gptworker
    spec:
      containers:
        - name: gptworker
          image: ghcr.io/woosung142/k-animator/gptworker:main
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: gpt-secrets
      imagePullSecrets:
        - name: ghcr-secret