requests==2.31.0                 # DALL·E API 호출 및 이미지 다운로드
Pillow==10.2.0                   # 이미지 처리 (PNG 저장, PSD 변환용 임시 이미지)
celery==5.3.6                    # 워커 태스크 처리
redis==5.0.3                     # 브로커/백엔드로 Redis 사용
azure-storage-blob==12.19.1     # Azure Blob Storage 업로드
python-dotenv==1.0.1            # .env 환경변수 로드
openai==1.93.3                   # 최신 OpenAI SDK (Azure 포함)
